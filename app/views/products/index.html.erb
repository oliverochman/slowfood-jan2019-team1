<table>
  <% @products.each do |product| %>
    <tr id=<%= dom_id(product) %> >
      <td><%= product.name %></td>
      <td><%= product.description %></td>
      <td><%= product.price %></td>
    </tr>
  <% end %>
</table>